# Preferences

# Set to True if you have a slow Internet connection, it will stream videos in 360p = False
low_mode = True

# Time before it start streaming the video, for preloading. Useful if you have a slow internet connection. Default is 0.
time_preloading = 10

# Audio output : hdmi (default) or local (using the jack)
audio_output = "hdmi"
